import 'package:flutter/material.dart';

class FinalPage extends StatelessWidget {
  final String eventId;
  const FinalPage({required this.eventId, super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('FinalPage'),
    );
  }
}
